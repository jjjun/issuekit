Read the review prompt at: $prompt_path and respond with exactly one fenced review block containing a JSON object with keys verdict, verification, and notes. Do not implement code; do not modify files; do not mutate the tracker.
