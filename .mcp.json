{
  "mcpServers": {
    "issuekit": {
      "command": "uv",
      "args": [
        "run",
        "--group",
        "mcp",
        "issuekit-mcp"
      ],
      "cwd": "${workspaceFolder}"
    }
  }
}
